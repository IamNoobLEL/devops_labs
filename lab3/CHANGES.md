# Что изменилось относительно lab №2

- Добавлен GitLab CI/CD пайплайн
- Добавлен `test` с проверкой директорий `dags/` и `spark/`
- Добавлен `build` для сборки Docker image
- Добавлен `deploy` для запуска приложения через docker-compose
- Деплой автоматически запускается только в ветках `main`, `master` и `develop`
- Для веток `feature/*` build запускается только вручную
- Все джобы выполняются на runner с тегом `devops-lab3`
